@(Html.EJS().AccumulationChart("container")
    .Series(series =>
    {
        series.XName("xValue")
            .YName("yValue")
            .DataSource(ViewBag.dataSource)
            .Add();
    })
    .Render()
)