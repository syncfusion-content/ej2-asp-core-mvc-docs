@using Syncfusion.EJ2
@using Syncfusion.EJ2.Popups

@Html.EJS().Tooltip("Tooltip")
    .Content("Lets go green & Save Earth !!!")
    .ContentTemplate(@<span id='target'>Show Tooltip</span>)
    .Position(Position.TopCenter)
    .Render()
 
<style>
    #Tooltip {
        position: absolute;
        left: calc(50% - 60px);
        top: 38%;
    }
</style>